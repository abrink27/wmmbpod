<% @posts.each do |post| %>
  <div class="booyah-box col-xs-10 col-xs-offset-1">
    <h1><%= post.name %></h1><br />
    <p><%= post.description %></p>
    <i><%= "-" + post.poster %></i><br />
  </div>
<% end %>